// Test: Basic match expression with wildcard
// Expected: 42
// NOTE: Identifier patterns in match not yet working in semantic analysis
fn main() -> i32 {
    let x = 42;
    return match (x) {
        _ => x,
    };
}
